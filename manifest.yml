---
declared-services:
  conversation-service:
    label: conversation
    plan: free
  tone-analyzer:
    label: tone_analyzer
    plan: free
  cloudant-db:
    label: cloudantNoSQLDB
    plan: Lite

applications:
- path: .
  memory: 256M
  instances: 1
  name: DevConnectTelcoChatBot
  host: DevConnectTelcoChatBot
  disk_quota: 1024M
  services:
    - conversation-service
    - tone-analyzer
    - cloudant-db
