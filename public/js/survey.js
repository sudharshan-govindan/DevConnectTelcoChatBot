var Survey = (function() {
  // Publicly accessible methods defined
  return {
    survey:survey
  };
  
  function survey(surveyDetails)
  	
  		
   		
  		Api.logSurvey(JSON.stringify(surveyDetails));
  	
  		
  		//window.location='/index.html';
  	
  	
  }
    }());